export const metadata = {
  title: "Blogs",
  description: "This is blog page",
};

const BlogPage = () => {
  return (
    <div>
      <h1>This is Blog Page</h1>
    </div>
  );
};

export default BlogPage;
