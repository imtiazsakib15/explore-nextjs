export const metadata = {
    title: "Contact",
    description: "This is contact page",
  };

const ContactPage = () => {
    return (
        <div>
            This is Contact page
        </div>
    );
};

export default ContactPage;